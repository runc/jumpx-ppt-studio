# deepagents 引擎 + LangGraph 本地 server + OpenAI 兼容 model 包
deepagents>=0.6.7
langgraph-cli[inmem]>=0.2.0
langchain-openai>=0.3.0
python-dotenv>=1.0.0
# 导出（Phase 4b-1）：无头 Chromium 渲染 HTML deck → PDF/PNG。
# 装完需再跑一次：`playwright install chromium`
playwright>=1.40
# 图片忠实版 PPTX（Phase 4b-2）：每页截图铺满一张 16:9 幻灯片
python-pptx>=1.0
# 多格式资料解析（Phase 8b）：PDF/Word/PPTX/Excel/HTML → Markdown，喂进 Context Pack。
# 离线、MIT、核心无 ML 依赖；不装 [all] 以免引入 azure/audio。
markitdown[pdf,docx,pptx,xlsx]>=0.1.0
