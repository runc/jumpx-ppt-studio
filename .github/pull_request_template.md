<!-- 谢谢 PR！注意：幻灯生成逻辑在 jumpx-ppt-forge，不在本仓。 -->

## 改了什么

<!-- 一句话 -->

## 哪一块

- [ ] 前端 / 交互（`frontend/app/`）
- [ ] 后端 / 编排 / API（`backend/`）
- [ ] 渲染 / 导出（Playwright，`backend/export_deck.py` / `render_check.py`）
- [ ] 样式导入
- [ ] 部署 / Docker / 文档

## 自检

- [ ] 本地 `docker compose up --build` 起得来 / `RUN.md` 跑通
- [ ] 未提交密钥（`backend/.env`）或运行产物（`backend/workspace/`）
- [ ] 没有把"生成逻辑"塞进 Studio（那是 forge 的事）
- [ ] 如改了 skill 版本：通过 `SKILL_REF` 钉 tag，未 vendor 拷贝

## 备注 / 截图
