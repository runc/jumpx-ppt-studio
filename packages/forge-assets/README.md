# @jumpx/forge-assets

幻灯 skill 静态副本，供浏览器 Slide Studio Lite 版挂载。

```bash
pnpm sync:skill          # 从 GitHub 拉 v1.1.0（与 Dockerfile 一致）
FORCE_SYNC=1 pnpm sync:skill
```

产物在 `ai-slide-producer/`（gitignore，不入库）。
